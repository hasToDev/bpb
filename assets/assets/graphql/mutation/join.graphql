mutation Join(
    $playerId: String!,
    $playerName: String!,
    $version: String!,
) {
    join (
        playerId: $playerId,
        playerName: $playerName,
        version: $version,
    )
}